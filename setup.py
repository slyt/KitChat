from setuptools import setup

setup(  name='kitchat',
        version='1.0',
        description='The world\'s best chat client',
        install_requires=[
            'pyglet',
        ],
        zip_safe=False)
